<main>
  <ul class="inventories w-[70%] mx-auto divide-y divide-gray-300">
    <% @inventory_list.each do |invent_item| %>
      <%= render partial: 'shared/inventory_or_recipe_item',
        locals: { data: invent_item }
      %>
    <% end %>
</ul>
</main>